#include <stdio.h>

int pal(int n, int arr[n], int i, int flag){
	if(i==n/2 && flag){
		printf("The given integer is a Palindrome.");
	} else {
		if(arr[])
	}
}

int main(void){

	int n;
	printf("Enter the number of digits: ");
	scanf("%d", &n);
	char arr[n];
	printf("Enter the number: ");
	for(int i=0; i<n; i++){
		int x;
		scanf(" %1d", &x);
		arr[i]=x;
	}

	printf("\n");
	return 0;
}